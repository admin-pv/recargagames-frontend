# Modelo do .env dos scripts do motor comercial.
# Copie para a RAIZ do repo como .env (já está no .gitignore) e preencha.
#
# A secret key ignora RLS e lê custo de fornecedor e margem. Ela NUNCA vai
# para o repo, para o browser ou para uma Function do site — é a mesma
# classe de credencial que o Netlify guarda como STOREFRONT_SECRET_KEY.

SUPABASE_URL=https://ashmirzgyuhspymldpfv.supabase.co
SUPABASE_SECRET_KEY=
